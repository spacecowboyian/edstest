<% const title = "Popover"; %>

<title><%= title %></title>

<section class="example-page">

  <%- include("_header.ejs", {
    classes: ["example-page_#header"],
    title,
    stability: 2,
    summary: "Popovers are floating containers that appear when the user interacts with a UI component. They provide the user with additional content without cluttering the page."
  }) _%>

  <div class="example-page_#content">
    <%
      
      ["", "-start", "-end"].forEach(alignment => {
        ["top", "right", "bottom", "left"].forEach(side => {
          const modifier = side + alignment;
    %>
    <div class="inline-flex items-center mr-16 h-64">
      <div class="eds-popover eds-popover.<%= modifier %>" role="dialog">
        <div class="eds-popover_#content">Popover with <code><%= modifier %></code> modifier</div>
        <div class="eds-popover_#marker"></div>
      </div>
    </div>
    <%
        })
    %>
    <%
      });
    %>

    <%
      ["reduced-padding", "no-padding"].forEach(modifier => {
    %>
    <div class="inline-flex items-center mr-16 h-64">
      <div class="eds-popover eds-popover.top eds-popover.<%= modifier %>" role="dialog">
        <div class="eds-popover_#content">Popover with <code><%= modifier %></code> modifier</div>
        <div class="eds-popover_#marker"></div>
      </div>
    </div>
    <%
      });
    %>
  </div>

</section>
